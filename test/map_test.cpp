#include <unordered_map>
#include <fstream>

void PrintMap() {
    // std::unordered_map<std::string, uint64_t> um = {
    //     {"apple", 1},
    //     {"banana", 2},
    //     {"cherry", 3}
    // };

    // std::string expected_output = "Hello apple: 1\nHello banana: 2\nHello cherry: 3\n";

    // // Step 1: Write the map to the file
    // std::ofstream ofs("hello.txt");
    // write_out_map("Hello ", um, &ofs);
    // ofs.close();

    // // Step 2: Read the contents of the file
    // std::ifstream ifs("hello.txt");
    // std::stringstream buffer;
    // buffer << ifs.rdbuf();
    // std::string file_content = buffer.str();

    // //Step 3: Check if the content matches the expected output
    // if(file_content != expected_output) {
    //     exit(1);
    // }
    
}